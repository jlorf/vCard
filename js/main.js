// File: /mi-vcard/js/main.js

// Add event listeners to the phone number link
const phoneNumberLink = document.getElementById('phone-number-link');
phoneNumberLink.addEventListener('click', () => {
  // Code to handle phone number link click
});

// Add event listeners to the email link
const emailLink = document.getElementById('email-link');
emailLink.addEventListener('click', () => {
  // Code to handle email link click
});

// Add event listeners to the WhatsApp link
const whatsappLink = document.getElementById('whatsapp-link');
whatsappLink.addEventListener('click', () => {
  // Code to handle WhatsApp link click
});

// Add event listeners to the social media links
const socialMediaLinks = document.querySelectorAll('.social-media-link');
socialMediaLinks.forEach((link) => {
  link.addEventListener('click', () => {
    // Code to handle social media link click
  });
});